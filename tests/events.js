var assert = require('assert');
var fs = require('fs');
const SGEvents = require('../src/events');

describe('smartglassEmitter', function(){
    it('should create a new smartglassEmitter instance', function(){
        var smartglassEmitter = SGEvents()
    });
})
